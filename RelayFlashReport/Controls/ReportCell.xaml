<?xml version="1.0" encoding="UTF-8"?>
<ContentView	xmlns="http://xamarin.com/schemas/2014/forms"
							xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
							x:Class="RelayFlashReport.ReportCell">
	<ContentView.Content>
		<Grid x:Name="Layout">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="3*"/>
				<ColumnDefinition Width="2*"/>
				<ColumnDefinition Width="2*"/>
			</Grid.ColumnDefinitions>

			<Label x:Name="LabelNumber" Grid.Column="0" Text="{Binding Number}" VerticalOptions="Center" HorizontalOptions="Center" FontSize="Medium" TextColor="Navy"/>
			<Entry x:Name="EntryName" Grid.Column="1" Text="{Binding Name}" VerticalOptions="Center" HorizontalOptions="StartAndExpand" TextColor="Navy"/>
			<Label x:Name="LabelLap" Grid.Column="2" Text="{Binding Lap}" VerticalOptions="Center" HorizontalOptions="CenterAndExpand" FontSize="Medium" TextColor="Navy"/>
			<Label x:Name="LabelTotal" Grid.Column="3" Text="{Binding Total}" VerticalOptions="Center" HorizontalOptions="CenterAndExpand" FontSize="Medium" TextColor="Navy"/>
		</Grid>
	</ContentView.Content>
</ContentView>
